# dev 환경에서는 vite proxy 가 /api → http://localhost:4000 으로 보내므로 비워둬도 됨.
# 별도 도메인의 API 를 직접 호출하고 싶을 때만 절대 URL 지정.
# VITE_API_BASE_URL=http://localhost:4000/api

# vite.config.ts proxy 의 백엔드 타겟 (기본값 http://localhost:4000)
# VITE_API_PROXY_TARGET=http://localhost:4000
